export interface Product {
    name: string
    id: number
    price: number
    _id: string
}

export interface NewProduct {
    name: string
    id: number
    price: number
}

export interface UpdateProduct {
    name: string
    id: number
    price: number
    _id: string
}